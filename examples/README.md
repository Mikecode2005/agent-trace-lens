# Examples

All example files are synthetic. They cover successful agents, tool errors, running spans, retries, redaction metadata, delayed freshness, and OTLP mapping fragments. Do not replace them with production traces.
