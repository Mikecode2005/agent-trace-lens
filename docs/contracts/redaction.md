# Redaction contract

Redaction happens before export or rendering. The dashboard should represent redacted values as [REDACTED] and keep the policy name in metadata without retaining the original value.
