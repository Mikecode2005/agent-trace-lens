# Normalized span contract

The normalized span is the UI boundary: identity, trace grouping, name, service, kind, status, start time, duration, token count, cost, and a human-readable detail field. Attributes remain optional and should be redacted before display.
