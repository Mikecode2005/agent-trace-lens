# Status values

ok means the span completed without a recorded error. error means the span reported a failure or timeout. running means the span has not completed. Unknown provider statuses should be normalized explicitly rather than rendered as success.
