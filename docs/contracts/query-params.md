# Query parameters

q searches name, service, trace ID, and detail. status accepts all, ok, error, or running. service accepts all or a normalized service name. trace focuses the related span group and anchors to the investigation panel.
