# Export response

The export response is an object containing mode, generatedAt, and spans. The mode is required because consumers must know whether the response is sample, live, cached, or delayed.
