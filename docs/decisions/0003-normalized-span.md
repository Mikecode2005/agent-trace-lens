# Decision 0003: normalized span shape

Status: accepted

The UI depends on a compact normalized span shape so future OTLP, MCP, and provider adapters can converge without rewriting investigation views.
