# Decision 0002: label sample data

Status: accepted

Synthetic telemetry is useful for demonstrating the UI but must never look live. The dashboard shows a sample dataset badge and exposes mode metadata in exports.
