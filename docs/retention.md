# Retention

The dashboard should default to short-lived local retention for traces. Retention configuration must state whether spans, raw attributes, prompts, and exported reports are retained separately. Deletion should be explicit and observable.
