# OpenTelemetry Collector

A local collector can receive OTLP HTTP, apply resource detection and redaction processors, then forward normalized spans to the dashboard adapter. Keep collector configuration separate from application source.
