# MCP tool spans

MCP spans should retain server identity, tool name, request duration, result status, retry count, and a redacted argument summary. Never assume tool results are safe to render without a policy.
