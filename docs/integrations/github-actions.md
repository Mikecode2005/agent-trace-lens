# GitHub Actions integration

A future workflow can publish a redacted trace report as an artifact after integration tests. The workflow should fail on ingestion-contract regressions while keeping production traces out of public logs.
