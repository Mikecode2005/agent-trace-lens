# Client integration notes

Client adapters should record model calls and tool handoffs without coupling the dashboard to one vendor. Provider-specific metadata belongs in attributes and should be treated as optional.
