# Review guide

Reviewers should check freshness labels, empty and failure states, keyboard access, export privacy, type safety, and whether aggregates correspond to the filtered data. A polished UI is not enough if provenance is unclear.
