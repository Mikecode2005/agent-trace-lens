# Style guide

Prefer readable route modules, small pure helpers, explicit data mode labels, and accessible text paired with visual status. Avoid hidden fallback data and avoid introducing a provider dependency without a documented boundary.
