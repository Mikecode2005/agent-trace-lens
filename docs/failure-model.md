# Failure model

Differentiate provider errors, tool errors, timeouts, retries, canceled spans, and missing telemetry. The UI should show an ingestion error when the source is unavailable instead of rendering a zero-count success state.
