# Metrics

Track trace count, span count, error count, p50, p95, maximum duration, total tokens, and estimated cost. Aggregates must be computed from the same filtered set that the user is inspecting.
