# Investigation workflow

Start with an aggregate outlier, focus the trace, compare parent and child spans, inspect retries, then export a redacted artifact. Each step should retain the freshness label.
