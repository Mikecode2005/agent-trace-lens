# Trace tree

Parent-child relationships let users see whether time was spent in the model, orchestration layer, or tool. A missing parent should be visible as incomplete topology, not silently reparented.
