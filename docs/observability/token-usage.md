# Token usage

Input and output token counts should remain separate in the ingestion contract, even if the overview displays a total. Missing token data is unknown, not zero, unless the provider guarantees zero.
