# Privacy model

Prompts and tool arguments can contain personal or business-sensitive data. Local-first operation is the default. Future ingestion should support field allowlists, hashable identifiers, configurable redaction, and a clear retention policy before any remote storage is introduced.
