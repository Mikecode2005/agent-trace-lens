# Export contract

The sample export endpoint returns JSON with mode, generatedAt, and spans. A live implementation should preserve the same envelope, add a freshness timestamp, and make redaction policy visible. Export filenames should never imply that sample data is production telemetry.
