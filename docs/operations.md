# Operations

When live ingestion lands, monitor collector reachability, normalization failures, span volume, dropped attributes, and export latency. Dashboard health must be separate from agent health so operators can distinguish an outage from a quiet workload.
