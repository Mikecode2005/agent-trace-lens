# Security policy

Trace data can contain prompts, identifiers, tool arguments, and business context. The current dashboard is sample-only and does not ingest external data. Report sensitive issues privately and remove secrets from reproduction artifacts.
