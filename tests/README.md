# Test fixtures

Fixtures are intentionally synthetic and are organized around one investigation behavior per file. When a new adapter is added, include normalization cases, missing-field behavior, freshness mode, and redaction expectations before wiring it into the UI.
